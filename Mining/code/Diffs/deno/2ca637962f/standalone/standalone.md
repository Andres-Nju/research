File_Code/deno/2ca637962f/standalone/standalone_after.rs --- Rust
49 /// produced by `deno compile`. It determines if this is a stanalone                                                                                      49 /// produced by `deno compile`. It determines if this is a standalone

