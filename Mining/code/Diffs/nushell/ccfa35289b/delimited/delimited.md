File_Code/nushell/ccfa35289b/delimited/delimited_after.rs --- Rust
113             if !seen.contains(&desc) {                                                                                                                   113             if !desc.is_empty() && !seen.contains(&desc) {

