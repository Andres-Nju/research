File_Code/solana/d82ec2634c/fullnode/fullnode_after.rs --- Rust
60         let bank = Bank::new_default(leader_addr.is_some());                                                                                              60         let bank = Bank::new_default(leader_addr.is_none());

