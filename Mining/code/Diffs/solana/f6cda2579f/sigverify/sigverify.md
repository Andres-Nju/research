File_Code/solana/f6cda2579f/sigverify/sigverify_after.rs --- Rust
601         assert!(tx_bytes.len() < PACKET_DATA_SIZE);                                                                                                      601         assert!(tx_bytes.len() <= PACKET_DATA_SIZE);

