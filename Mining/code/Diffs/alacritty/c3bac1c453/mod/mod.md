File_Code/alacritty/c3bac1c453/mod/mod_after.rs --- Rust
1705         let end = min(start + count, self.grid.num_cols() - 1);                                                                                         1705         let end = min(start + count, self.grid.num_cols());

