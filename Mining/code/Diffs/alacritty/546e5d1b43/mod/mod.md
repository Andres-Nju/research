File_Code/alacritty/546e5d1b43/mod/mod_after.rs --- Rust
2103             let iter = terminal.renderable_cells(&config, None);                                                                                        2103             let iter = terminal.renderable_cells(&config, None, false);

