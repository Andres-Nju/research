File_Code/rust/dd702cc653/pattern/pattern_after.rs --- Rust
8         let arr = [$( Step::from(searcher.$func()) ),+];                                                                                                   8         let arr = [$( Step::from(searcher.$func()) ),*];

