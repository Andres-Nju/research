File_Code/rust/66a47182d1/builder/builder_after.rs --- Rust
780         assert!(!use_snapshot || stage == 0);                                                                                                            780         assert!(!use_snapshot || stage == 0 || self.local_rebuild);

