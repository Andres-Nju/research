File_Code/rust/425c2c3606/coherence/coherence_after.rs --- Rust
280 ///    We need to be able to prove that `Option<$0>: !Iterator` for every type $0.                                                                       280 ///    We need to be able to prove that `Vec<$0>: !Iterator` for every type $0.

